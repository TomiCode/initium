{{template "header"}}
<!-- Menubar -->

<div class="ui container">
  <h1>Initium</h1>
</div>

<div class="ui secondary general menu">
  <div class="ui container">
    <div class="item">
      <img src="logo2.png" />
    </div>

    {{if .Current}}
      <div class="ui pointing dropdown item">
        <i class="cube icon"></i>{{.Current.Header.Name}} {{if .Current.Options}}<i class="dropdown icon"></i>{{end}}
        <div class="menu">
          <a class="item" route="{{.Current.Header.Route}}">Index</a>
          {{range .Current.Options}}
            <div class="divider"></div>
            {{if .Name}}
              <div class="header">{{.Name}}</div>
            {{end}}
            {{range .Collection}}
              <a class="item" route="{{.Route}}">{{.Name}}</a>
            {{end}}
          {{end}}
        </div>
      </div>
    {{end}}

    {{if .Elements}}
      <div class="ui poiniting dropdown item">
        Modules <i class="dropdown icon"></i>
        <div class="menu">
          <div class="header">Your modules</div>
          {{range .Elements}}
            <a class="item" route="{{.Route}}">{{.Name}}</a>
          {{end}}
        </div>
      </div>
    {{end}}
    
    <div class="right menu">
      <a href="/auth" class="item">
        <img class="ui avatar image" src="test.png" />
        <span>Username</span>
        <i class="dropdown icon"></i>
      </a>
    </div>
  </div>
</div>
